# Existing Features

> What has actually shipped and been accepted. Updated by `/sdlc:review` at iteration close. Read by humans (and `/sdlc:plan`) to know what's already there.

## Shipped

_Each entry is one feature, in plain language, with the iteration that landed it._

<!-- Example:
- **CSV export** — users can export their data as CSV from settings → export (iteration 0001)
-->
